# macOS / OS junk (keep it repo-local so it can never be committed and then
# shipped inside the addon zip)
.DS_Store
*.bak
*.swp

# Compiled Lua sources
luac.out

# BigWigs packager output
.release/
*.zip
